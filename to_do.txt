For the odyssey app:

- Add project id to project details in the settings tab. Make it so a project owner can change the project id code. This code makes it so a new user could add that project using the project id  code. If the project id code changes and someone tries to use an old code it should not work. Make the id code long enough that no one could reasonable guess the code or type in a random code and join a random project.  

- Fix the project persistence after deletion issue which arises when i delete a project from the website project tab, go to dashboard, come back to projects the deleted project is still there.

- Make all file names from all repos and sources clickable and viewable in the ai insight, chat windows, intelligent update window, and anywhere else markdown text is output. Make it so that when i click a repo and its showing on the left hand side of the screen and then i click a file in the repo that it extends the file window from the repo pane to the right hand side of the page. 

- Show the ai chat icon at all times, even when in the dashboard and not in a project. Based on the prompt a user enters into the chat the ai chatbot should be able to know what project the user is referring to. 

- Make the preview window wider for adding context when i am adding any type of file, document, repo into the ai chatbot on the right hand pane. have it be for the main context type selection window, and the window another layer down for adding the specific file, repo, etc.

- If someone enters text into the ai chat window within the reports tab, have that prompt combine with the default report generation prompt in a logical way and have that be what the report generation prompt gets sent as. Display the logically formed combined prompt within the chat window. Make sure that the user maintains the ability to edit a report within that chat window. If they do not like the first result and say something like, "actually make it as a pdf instead of a pptx", the chat should regenerate the report as a pdf and provide it within the chat window.

- All chatbot responses within chat windows (reports tab window, project ai chat window) should have formatted text. 

-  Make it so in the projects page I can reorder projects by name or recency of creation in ascending or descending order. Make it so i can click and drag them to rearrange in my preferred order as well. Have that ordering reflect in the dashboard where the project names are shown in a bar at the top. Make it so i dont have to click custom to be able to drag and reorder but that the system knows and reflects on the button highlighting color indicator that its a custom order.

- Add a USA them that is deep navy blue background with white text and red accents. Fill out other colors appropriately. Theme preview colors should be red, white, blue in that order with name USA.

^done with codex 5.4

- 